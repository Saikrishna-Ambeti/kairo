---
import Layout from "../layouts/Layout.astro";

const providers = [
  {
    name: "Claude Code",
    tagline: "Anthropic Claude 3.7 Sonnet with native subagents & deep reasoning",
    cmd: "claude auth login",
  },
  {
    name: "Codex CLI",
    tagline: "OpenAI Codex agent with native JSON-RPC app-server support",
    cmd: "codex login",
  },
  {
    name: "OpenCode",
    tagline: "Open-source, self-hosted LLM endpoints and local models",
    cmd: "opencode auth login",
  },
];

const appModes = [
  {
    id: "canvas",
    name: "Calm Canvas",
    label: "Minimal",
    desc: "A distraction-free prompt bar with memory suggestions.",
  },
  {
    id: "workspace",
    name: "Workspace Rails",
    label: "Threads & Memory",
    desc: "Multi-thread sidebar with persistent memory tags always visible.",
  },
  {
    id: "briefing",
    name: "Today Briefing",
    label: "Action Cards",
    desc: "Actionable cards for pending replies, meeting prep, and weekly tasks.",
  },
  {
    id: "split",
    name: "Split Inspector",
    label: "Traceable AI",
    desc: "Synthesized answers on the left, citations and verified memory on the right.",
  },
];
---

<Layout
  title="Kairo · A calmer way to get through everyday work"
  description="One place to ask for help, keep useful context in persistent memory, and connect the assistant to the tools you already use."
>
  <!-- ══════════════════════════════════════════════════════════
       1. HERO SECTION
       ══════════════════════════════════════════════════════════ -->
  <section class="k-hero-stage" id="hero">
    <div class="k-container k-hero-container">
      <div class="k-hero-layout-grid">
        <div class="k-hero-left">
          <!-- Status Badge -->
          <div class="k-live-badge k-reveal">
            <span class="k-pulse-dot"></span>
            <span class="k-badge-text">AGENTIC CONTROL PLANE &middot; ONLINE</span>
          </div>

          <!-- Headline -->
          <h1 class="k-hero-title k-reveal">
            A calmer way to get<br />
            <span class="k-title-gradient">through everyday work.</span>
          </h1>

          <!-- Subtitle -->
          <p class="k-hero-subtitle k-reveal">
            One place to ask for help, keep useful context in persistent memory, and connect your assistant directly to the tools you already use.
          </p>

          <!-- CTA Action Buttons -->
          <div class="k-hero-actions k-reveal">
            <a href="#download" class="k-btn k-btn--primary k-btn--lg">
              <span class="symbol">download</span>
              <span>Download for Desktop</span>
            </a>
          </div>

          <div class="k-hero-trust-row k-reveal">
            <span class="k-trust-item"><span class="symbol icon-blue" style="font-size:16px">verified</span> Zero token markup</span>
            <span class="k-trust-item"><span class="symbol icon-green" style="font-size:16px">lock</span> Local stdio security</span>
            <span class="k-trust-item"><span class="symbol icon-purple" style="font-size:16px">sync</span> Persistent memory</span>
          </div>
        </div>

        <div class="k-hero-right">
          <!-- Live Interactive Prompt Simulator -->
          <div class="k-prompt-sim-card k-reveal-scale">
            <div class="k-prompt-sim-top">
              <div class="k-window-dots">
                <span class="dot red"></span>
                <span class="dot yellow"></span>
                <span class="dot green"></span>
              </div>
              <div class="k-sim-engine-pills">
                <button class="k-engine-pill active" data-engine="Claude Code 3.7">Claude 3.7</button>
                <button class="k-engine-pill" data-engine="Codex CLI">Codex</button>
                <button class="k-engine-pill" data-engine="OpenCode">OpenCode</button>
              </div>
            </div>
            <div class="k-prompt-input-row">
              <div class="k-prompt-icon-bubble">
                <span class="symbol">auto_awesome</span>
              </div>
              <input
                type="text"
                id="k-sim-input"
                class="k-sim-text-input"
                value="Summarize my unread emails and draft replies in my voice"
                readonly
              />
              <button class="k-sim-send-btn" id="k-sim-send" title="Run prompt">
                <span class="symbol">arrow_upward</span>
              </button>
            </div>
            <div class="k-prompt-chips">
              <span class="k-chip-label">Quick suggestions:</span>
              <button class="k-prompt-chip active" data-prompt="Summarize my unread emails and draft replies in my voice">summarize inbox</button>
              <button class="k-prompt-chip" data-prompt="Draft a calm vendor negotiation response using last week's contract notes">draft reply</button>
              <button class="k-prompt-chip" data-prompt="Plan my sprint schedule based on Linear tickets and upcoming calendar meetings">plan my week</button>
            </div>
            <div class="k-prompt-sim-result" id="k-sim-result">
              <div class="k-sim-res-header">
                <span class="k-memory-tag">
                  <span class="symbol" style="font-size:12px">save</span>
                  <span>Using memory: Prefers concise bullet points &middot; PST timezone</span>
                </span>
                <span class="k-sim-time">Just now</span>
              </div>
              <p class="k-sim-res-text" id="k-sim-text">
                Found <strong>4 actionable emails</strong>. Prepared 2 quick drafts ready for review, flagged 1 urgent contract update from finance, and cleared the remaining 5 notifications.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════════
       2. VIDEO SHOWCASE (SEE KAIRO IN A MINUTE)
       ══════════════════════════════════════════════════════════ -->
  <section class="k-section k-video-section" id="trailer">
    <div class="k-container">
      <div class="k-section-header k-text-center k-reveal">
        <span class="k-section-pill">60-Second Overview</span>
        <h2 class="k-section-heading">See Kairo in a minute</h2>
        <p class="k-section-lead">
          Ask for help, keep context forever, connect your daily tools. Zero setup theatre.
        </p>
      </div>

      <div class="k-video-player-wrapper k-reveal-scale">
        <div class="k-video-glow"></div>
        <div class="k-video-frame">
          <video
            id="k-main-video"
            src="/uploads/kairo-promo-v2.mp4"
            playsinline
            autoplay
            muted
            loop
            preload="auto"
            class="k-hero-video"
          ></video>
        </div>
      </div>

      <!-- 3 Highlights under Video -->
      <div class="k-video-bullets-grid">
        <div class="k-bullet-card k-reveal">
          <div class="k-bullet-icon">
            <span class="symbol">bolt</span>
          </div>
          <h4>Zero Token Markup</h4>
          <p>Connect directly to your existing API keys and local CLI agents via standard stdio JSON-RPC.</p>
        </div>

        <div class="k-bullet-card k-reveal">
          <div class="k-bullet-icon">
            <span class="symbol">memory</span>
          </div>
          <h4>Supermemory Engine</h4>
          <p>Context, rules, and preferences persist effortlessly across conversations and restarts.</p>
        </div>

        <div class="k-bullet-card k-reveal">
          <div class="k-bullet-icon">
            <span class="symbol">security</span>
          </div>
          <h4>Local-First &amp; Private</h4>
          <p>Files, credentials, and code never pass through proprietary proxy servers. You stay in control.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════════
       3. SPECIALITIES & FEATURES BENTO GRID
       ══════════════════════════════════════════════════════════ -->
  <section class="k-section k-features-section" id="features">
    <div class="k-container">
      <div class="k-features-top-bar k-reveal">
        <div>
          <span class="k-section-pill">Core Intelligence</span>
          <h2 class="k-section-heading">Engineered for deep work &amp; everyday tasks</h2>
        </div>
        <p class="k-features-subhead">
          Calm, traceable, and private. Connect your tools, retain context effortlessly, and execute with precision.
        </p>
      </div>

      <div class="k-bento-grid">
        <!-- Feature 1: Understand Documents -->
        <div class="k-bento-card k-reveal" data-spotlight>
          <div class="k-card-header">
            <div class="k-icon-box k-icon-purple">
              <span class="symbol">description</span>
            </div>
            <span class="k-card-tag">Document Intelligence</span>
          </div>
          <h3 class="k-card-title">Understand complex files</h3>
          <p class="k-card-desc">
            Drop in spreadsheets, PDFs, or repositories to extract actionable insights in milliseconds without manual reading.
          </p>

          <!-- Rich Document Preview Widget -->
          <div class="k-doc-preview-widget">
            <div class="k-doc-header-row">
              <div class="k-doc-file-info">
                <span class="symbol k-pdf-icon">picture_as_pdf</span>
                <div>
                  <strong class="k-doc-filename">Q3_Vendor_SLA_Audit.pdf</strong>
                  <span class="k-doc-meta">42 pages &middot; 1.8 MB &middot; Verified</span>
                </div>
              </div>
              <span class="k-status-badge-green">Parsed</span>
            </div>
            <div class="k-doc-insights-list">
              <div class="k-insight-item">
                <span class="symbol k-check-icon">check_circle</span>
                <span>Guaranteed 99.95% uptime SLA clause confirmed</span>
              </div>
              <div class="k-insight-item">
                <span class="symbol k-check-icon">check_circle</span>
                <span>Rate lock secured through Q4 2027 ($12.4k savings)</span>
              </div>
              <div class="k-insight-item">
                <span class="symbol k-check-icon">check_circle</span>
                <span>Automatic egress fee waiver for cross-region backup</span>
              </div>
            </div>
            <div class="k-doc-footer-meta">
              <span class="symbol">bolt</span>
              <span>Extraction completed in 240ms &middot; 99.4% confidence</span>
            </div>
          </div>
        </div>

        <!-- Feature 2: Draft & Summarize -->
        <div class="k-bento-card k-reveal" data-spotlight>
          <div class="k-card-header">
            <div class="k-icon-box k-icon-blue">
              <span class="symbol">edit_note</span>
            </div>
            <span class="k-card-tag">Smart Synthesis</span>
          </div>
          <h3 class="k-card-title">Draft in your exact voice</h3>
          <p class="k-card-desc">
            Replies, briefs, notes, and action plans synthesized to match your custom communication style and audience.
          </p>

          <!-- Rich Tone Switcher Widget -->
          <div class="k-tone-widget">
            <div class="k-tone-switcher">
              <button class="k-tone-btn active" data-tone="short">Short</button>
              <button class="k-tone-btn" data-tone="detailed">Detailed</button>
              <button class="k-tone-btn" data-tone="casual">Casual</button>
              <button class="k-tone-btn" data-tone="executive">Executive</button>
            </div>
            <div class="k-tone-preview-box">
              <div class="k-tone-quote-row">
                <span class="symbol icon-blue" style="font-size:16px">format_quote</span>
                <span class="k-tone-meta-pill" id="k-tone-word-count">14 words &middot; ~2s read</span>
              </div>
              <p id="k-tone-preview-text">"Thanks for the update. All approved for Thursday's deploy."</p>
              <div class="k-tone-preview-footer">
                <span class="k-tone-tone-label">Tone: Direct &amp; Concise</span>
                <button class="k-copy-snippet-btn" id="k-copy-tone-btn" title="Copy preview text">
                  <span class="symbol">content_copy</span>
                  <span>Copy</span>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Feature 3: Persistent Supermemory -->
        <div class="k-bento-card k-reveal" data-spotlight>
          <div class="k-card-header">
            <div class="k-icon-box k-icon-green">
              <span class="symbol">psychology</span>
            </div>
            <span class="k-card-tag">Persistent Supermemory</span>
          </div>
          <h3 class="k-card-title">Context that endures</h3>
          <p class="k-card-desc">
            Preferences, style guides, and domain constraints persist automatically across conversations and IDE restarts.
          </p>

          <!-- Rich Supermemory Chips Widget -->
          <div class="k-memory-widget">
            <div class="k-memory-chips-demo">
              <div class="k-mem-pill active" data-mem-pill>
                <div class="k-mem-pill-left">
                  <span class="symbol mem-icon">check</span>
                  <div>
                    <strong class="k-mem-key">Format Style:</strong>
                    <span class="k-mem-val">Concise bullet points &middot; No fluff</span>
                  </div>
                </div>
                <span class="k-mem-category-tag">Persona</span>
              </div>
              <div class="k-mem-pill active" data-mem-pill>
                <div class="k-mem-pill-left">
                  <span class="symbol mem-icon">check</span>
                  <div>
                    <strong class="k-mem-key">Timezone:</strong>
                    <span class="k-mem-val">PST (UTC-8) &middot; Morning brief 8:30 AM</span>
                  </div>
                </div>
                <span class="k-mem-category-tag">Env</span>
              </div>
              <div class="k-mem-pill" data-mem-pill>
                <div class="k-mem-pill-left">
                  <span class="symbol mem-icon">add</span>
                  <div>
                    <strong class="k-mem-key">CI Rule:</strong>
                    <span class="k-mem-val">Require typecheck before merge</span>
                  </div>
                </div>
                <span class="k-mem-category-tag">Rules</span>
              </div>
            </div>
            <div class="k-memory-footer-note">
              <span class="symbol">sync</span>
              <span>Encrypted local storage &middot; Click pill to toggle active context</span>
            </div>
          </div>
        </div>

        <!-- Feature 4: Connected Data & Tools -->
        <div class="k-bento-card k-reveal" data-spotlight>
          <div class="k-card-header">
            <div class="k-icon-box k-icon-yellow">
              <span class="symbol">hub</span>
            </div>
            <span class="k-card-tag">Direct Integrations</span>
          </div>
          <h3 class="k-card-title">Connected tools ecosystem</h3>
          <p class="k-card-desc">
            Read issues, inspect commits, and draft pull requests without proxy servers or external token markups.
          </p>

          <!-- Rich Tools Ecosystem Widget -->
          <div class="k-tools-grid-preview">
            <div class="k-tool-chip">
              <span class="symbol icon-blue">terminal</span>
              <div class="k-tool-info">
                <strong>GitHub</strong>
                <span>Live repo sync</span>
              </div>
              <span class="k-tool-status-dot active"></span>
            </div>
            <div class="k-tool-chip">
              <span class="symbol icon-purple">linear_scale</span>
              <div class="k-tool-info">
                <strong>Linear</strong>
                <span>Sprint roadmap</span>
              </div>
              <span class="k-tool-status-dot active"></span>
            </div>
            <div class="k-tool-chip">
              <span class="symbol icon-green">forum</span>
              <div class="k-tool-info">
                <strong>Slack</strong>
                <span>Unread briefs</span>
              </div>
              <span class="k-tool-status-dot active"></span>
            </div>
            <div class="k-tool-chip">
              <span class="symbol icon-yellow">cloud</span>
              <div class="k-tool-info">
                <strong>Google Drive</strong>
                <span>12 indexed files</span>
              </div>
              <span class="k-tool-status-dot active"></span>
            </div>
          </div>
          <div class="k-tools-footer-badge">
            <span class="symbol">lock</span>
            <span>Local stdio subprocess access &middot; Zero third-party proxy</span>
          </div>
        </div>

        <!-- Feature 5: Multi-Provider Orchestration (Wide Card) -->
        <div class="k-bento-card k-bento-wide k-reveal" data-spotlight>
          <div class="k-wide-layout">
            <div class="k-wide-info">
              <div class="k-card-header">
                <div class="k-icon-box k-icon-purple">
                  <span class="symbol">auto_awesome_motion</span>
                </div>
                <span class="k-card-tag">Unified Control Plane</span>
              </div>
              <h3 class="k-card-title">Multi-provider agentic orchestration</h3>
              <p class="k-card-desc">
                Coordinate leading LLM engines under one interface with zero configuration overhead or lock-in.
              </p>
              <div class="k-wide-metrics-row">
                <div class="k-metric-pill">
                  <strong>0%</strong>
                  <span>Token Markup</span>
                </div>
                <div class="k-metric-pill">
                  <strong>&lt;5ms</strong>
                  <span>Socket Latency</span>
                </div>
                <div class="k-metric-pill">
                  <strong>100%</strong>
                  <span>Local Stdio</span>
                </div>
              </div>
            </div>
            <div class="k-provider-cards-col">
              {
                providers.map((p) => (
                  <div class="k-provider-mini-card">
                    <div class="k-pm-left">
                      <div class="k-pm-title-row">
                        <strong class="k-pm-name">{p.name}</strong>
                      </div>
                      <span class="k-pm-desc">{p.tagline}</span>
                    </div>
                    <div class="k-pm-right">
                      <span class="k-cli-shortcut-tag">{p.cmd}</span>
                    </div>
                  </div>
                ))
              }
            </div>
          </div>
        </div>
      </div>

      <!-- Early access banner -->
      <div class="k-info-banner k-reveal">
        <span class="symbol icon-purple">lightbulb</span>
        <div>
          <strong>Kairo 2.0 is actively evolving</strong> &middot; Transitioning from a developer-focused workbench into an everyday calming assistant experience.
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════════
       4. INTERACTIVE UI MODES EXPLORER
       ══════════════════════════════════════════════════════════ -->
  <section class="k-section k-modes-section" id="modes">
    <div class="k-container">
      <div class="k-section-header k-text-center k-reveal">
        <span class="k-section-pill">Interactive Experience</span>
        <h2 class="k-section-heading">Explore the 4 interface paradigms</h2>
        <p class="k-section-lead">
          Designed to adapt to how you work — from deep focus to high-level daily briefings.
        </p>
      </div>

      <!-- Mode Selector Tabs -->
      <div class="k-mode-tabs-row k-reveal" id="k-mode-tab-buttons">
        {
          appModes.map((mode, i) => (
            <button
              class={`k-mode-tab-btn ${i === 0 ? "active" : ""}`}
              data-mode={mode.id}
            >
              <span class="k-tab-pill">{mode.label}</span>
              <span class="k-tab-name">{mode.name}</span>
            </button>
          ))
        }
      </div>

      <!-- Mode Preview Container -->
      <div class="k-mode-stage-wrapper k-reveal-scale">
        <!-- 1. Calm Canvas View (1a) -->
        <div class="k-mode-view active" id="view-canvas">
          <div class="k-canvas-inner">
            <div class="k-canvas-header">
              <div class="k-app-brand">
                <img src="/logo.png" alt="Kairo" width="18" height="18" style="border-radius: 4px; object-fit: cover;" />
                <span>kairo</span>
                <span class="k-canvas-mode-tag">Calm Canvas</span>
              </div>
              <div class="k-canvas-actions">
                <span class="k-status-indicator"><span class="k-pulse-dot-sm"></span> Supermemory Connected</span>
                <span class="symbol">tune</span>
              </div>
            </div>
            <div class="k-canvas-body">
              <div class="k-canvas-intro">
                <span class="k-canvas-eyebrow">Good morning</span>
                <h3 class="k-canvas-greeting">What's on your plate today?</h3>
              </div>
              <div class="k-canvas-input-box">
                <div class="k-canvas-input-top">
                  <span class="symbol icon-purple" style="font-size:18px">auto_awesome</span>
                  <input type="text" placeholder="Ask a question, analyze a file, or describe a workflow..." value="Summarize unread engineering PRs and draft my weekly retro" readonly />
                </div>
                <div class="k-canvas-bottom-bar">
                  <div class="k-canvas-tools">
                    <span class="k-tool-btn"><span class="symbol">attach_file</span> Add File</span>
                    <span class="k-tool-btn"><span class="symbol">psychology</span> Memory Active</span>
                  </div>
                  <button class="k-canvas-send-btn" title="Submit prompt">
                    <span class="symbol">arrow_forward</span>
                  </button>
                </div>
              </div>
              <div class="k-canvas-chips-row">
                <span class="k-prompt-chip active">summarize my inbox</span>
                <span class="k-prompt-chip">draft a reply</span>
                <span class="k-prompt-chip">plan my week</span>
                <span class="k-prompt-chip">audit vendor contract</span>
              </div>
              <div class="k-canvas-result-card">
                <div class="k-canvas-result-head">
                  <span class="k-memory-tag"><span class="symbol" style="font-size:12px">verified</span> Context Synthesized</span>
                  <span class="k-sim-time">Just now</span>
                </div>
                <p class="k-canvas-result-desc">
                  Found <strong>3 pending PR reviews</strong> and <strong>2 blockers in Linear</strong>. Prepared draft review notes highlighting schema contract changes.
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- 2. Workspace Rails View (1b) -->
        <div class="k-mode-view" id="view-workspace">
          <div class="k-workspace-inner">
            <div class="k-ws-sidebar">
              <div class="k-app-brand">
                <img src="/logo.png" alt="Kairo" width="18" height="18" style="border-radius: 4px; object-fit: cover;" />
                <span>kairo</span>
              </div>
              <button class="k-new-btn">
                <span class="symbol">add</span>
                <span>New thread</span>
              </button>
              <div class="k-thread-list">
                <div class="k-thread-item active">
                  <div class="k-thread-title-row">
                    <span class="k-thread-status-dot green"></span>
                    <span class="k-thread-name">Cloudflare R2 vs S3</span>
                  </div>
                  <span class="k-thread-time">2m ago</span>
                </div>
                <div class="k-thread-item">
                  <div class="k-thread-title-row">
                    <span class="k-thread-status-dot purple"></span>
                    <span class="k-thread-name">Draft Sprint Retrospective</span>
                  </div>
                  <span class="k-thread-time">1h ago</span>
                </div>
                <div class="k-thread-item">
                  <div class="k-thread-title-row">
                    <span class="k-thread-status-dot blue"></span>
                    <span class="k-thread-name">Codebase Memory Sync</span>
                  </div>
                  <span class="k-thread-time">Yesterday</span>
                </div>
              </div>
              <div class="k-ws-bottom-connected">
                <span class="k-conn-label">Connected Tools &middot; Online</span>
                <div class="k-conn-chips-list">
                  <span class="k-conn-badge"><span class="symbol">code</span> GitHub</span>
                  <span class="k-conn-badge"><span class="symbol">forum</span> Slack</span>
                </div>
              </div>
            </div>
            <div class="k-ws-main">
              <div class="k-ws-chat-area">
                <div class="k-chat-bubble user">
                  <div class="k-bubble-author">You &middot; 10:42 AM</div>
                  Compare AWS S3 vs Cloudflare R2 storage pricing for our 12TB database archive.
                </div>
                <div class="k-chat-bubble assistant">
                  <div class="k-bubble-author-row">
                    <span class="k-bubble-author">Kairo &middot; Claude 3.7</span>
                    <span class="k-badge badge-green">Direct stdio</span>
                  </div>
                  <p>Based on your 12TB footprint with ~2TB/month egress:</p>
                  <div class="k-ws-pricing-table">
                    <div class="k-pricing-row highlight">
                      <span class="k-provider-name">Cloudflare R2</span>
                      <span class="k-provider-cost">~$180 / mo</span>
                      <span class="k-provider-note">Zero egress fees</span>
                    </div>
                    <div class="k-pricing-row">
                      <span class="k-provider-name">AWS S3 Standard</span>
                      <span class="k-provider-cost">~$456 / mo</span>
                      <span class="k-provider-note">$276 storage + $180 egress</span>
                    </div>
                  </div>
                  <div class="k-ws-saved-memory-alert">
                    <span class="symbol">bookmark</span>
                    <span>Saved to memory: Prioritizes zero-egress cloud storage solutions</span>
                  </div>
                </div>
              </div>
              <div class="k-ws-input-bar">
                <input type="text" placeholder="Reply or ask for refined migration script..." />
                <button class="k-canvas-send-btn"><span class="symbol">arrow_upward</span></button>
              </div>
            </div>
          </div>
        </div>

        <!-- 3. Today Briefing View (1c) -->
        <div class="k-mode-view" id="view-briefing">
          <div class="k-briefing-inner">
            <div class="k-briefing-top">
              <div>
                <div class="k-date-title" id="k-live-briefing-date">Daily Briefing &middot; Today</div>
                <span class="k-briefing-subtitle">3 priority action items identified from your connected feeds</span>
              </div>
              <div class="k-briefing-actions">
                <span class="k-status-badge-green">All Feeds Synced</span>
              </div>
            </div>
            <div class="k-briefing-cards-grid">
              <div class="k-brief-card">
                <div class="k-brief-badge red">Needs Reply</div>
                <h4>Engineering PR #412</h4>
                <p>Waiting on your review for schema migration bindings and JSON-RPC protocol guards.</p>
                <div class="k-brief-card-footer">
                  <button class="k-brief-action-btn">Draft review &rarr;</button>
                  <span class="k-brief-time">Due in 2h</span>
                </div>
              </div>

              <div class="k-brief-card">
                <div class="k-brief-badge blue">Meeting Prep</div>
                <h4>Product Roadmap Sync</h4>
                <p>3 key updates compiled from Linear tickets and unread Slack engineering threads.</p>
                <div class="k-brief-card-footer">
                  <button class="k-brief-action-btn">Open briefing &rarr;</button>
                  <span class="k-brief-time">At 2:00 PM</span>
                </div>
              </div>

              <div class="k-brief-card wide">
                <div class="k-brief-badge purple">Contract Intelligence</div>
                <h4>Vendor renewal contract analysis</h4>
                <p>Kairo finished cross-referencing SLA clauses with historical uptime metrics and identified $12.4k in potential annual savings.</p>
                <div class="k-brief-card-footer">
                  <button class="k-brief-action-btn">View comparison report &rarr;</button>
                  <span class="k-brief-time">Completed</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- 4. Split Inspector View (1d) -->
        <div class="k-mode-view" id="view-split">
          <div class="k-split-inner">
            <div class="k-split-left">
              <div class="k-split-pane-header">
                <h4>Synthesized Intelligence</h4>
                <span class="k-badge badge-blue">Verified Output</span>
              </div>
              <p class="k-split-lead">Here is what Kairo analyzed across your active repositories and documentation:</p>
              <div class="k-answer-box">
                <p>All contract schemas conform to <code>@kairo/contracts</code>. The stdio JSON-RPC server handles reconnect events gracefully without losing active thread contexts.</p>
                <div class="k-code-snippet-box">
                  <code>export const SessionEvent = Schema.Union(TaskCreated, SessionRestarted);</code>
                </div>
                <div class="k-answer-actions">
                  <button class="k-btn k-btn--sm"><span class="symbol">content_copy</span> Copy code</button>
                  <button class="k-btn k-btn--sm"><span class="symbol">tune</span> Refine prompt</button>
                </div>
              </div>
            </div>
            <div class="k-split-right">
              <div class="k-split-pane-header">
                <h4>Traceable Sources (2)</h4>
                <span class="k-badge badge-green">100% Local</span>
              </div>
              <div class="k-source-item">
                <span class="symbol icon-purple">article</span>
                <div>
                  <strong>schema.ts</strong>
                  <span>packages/contracts/src/schema.ts:14-28</span>
                </div>
              </div>
              <div class="k-source-item">
                <span class="symbol icon-blue">terminal</span>
                <div>
                  <strong>stdio-session.ts</strong>
                  <span>apps/server/src/stdio-session.ts:89-104</span>
                </div>
              </div>

              <div class="k-split-pane-header" style="margin-top: 20px;">
                <h4>Supermemory Applied</h4>
              </div>
              <div class="k-mem-used-box">
                <span class="symbol icon-green">psychology</span>
                <div>
                  <strong>TypeScript 5.8 &middot; Strict Mode</strong>
                  <span>Enforce type safety across all schema serializers</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════════
       5. DESKTOP INSTALLATION & PROVIDER QUICK-START
       ══════════════════════════════════════════════════════════ -->
  <section class="k-section k-download-section" id="download">
    <div class="k-container">
      <div class="k-download-layout">
        <!-- Left Side: OS Downloads -->
        <div class="k-download-info k-reveal">
          <span class="k-section-pill">Cross-Platform Desktop Apps</span>
          <h2 class="k-section-heading">Install Kairo on your machine</h2>
          <p class="k-download-lead">
            Available natively for Windows, macOS, and Linux. Built with local stdio for unmatched security and zero network token proxy latency.
          </p>

          <div class="k-os-cards-grid">
            <a href="/download" class="k-os-card k-reveal">
              <div class="k-os-icon">
                <span class="symbol">desktop_windows</span>
              </div>
              <div class="k-os-details">
                <strong class="k-os-title">Download for Windows</strong>
                <span class="k-os-meta">.exe installer &middot; x64 / ARM64</span>
              </div>
              <span class="symbol k-download-arrow">download</span>
            </a>

            <a href="/download" class="k-os-card k-reveal">
              <div class="k-os-icon">
                <span class="symbol">laptop_mac</span>
              </div>
              <div class="k-os-details">
                <strong class="k-os-title">Download for macOS</strong>
                <span class="k-os-meta">.dmg &middot; Apple Silicon &amp; Intel</span>
              </div>
              <span class="symbol k-download-arrow">download</span>
            </a>
          </div>

          <div class="k-github-releases-row k-reveal">
            <a href="https://github.com/pingdotgg/kairo/releases" target="_blank" rel="noreferrer" class="k-gh-link">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M12 .5C5.65.5.5 5.65.5 12a11.5 11.5 0 007.86 10.93c.58.1.79-.25.79-.56v-2c-3.2.69-3.88-1.37-3.88-1.37-.52-1.33-1.27-1.69-1.27-1.69-1.04-.71.08-.7.08-.7 1.15.08 1.76 1.18 1.76 1.18 1.02 1.76 2.68 1.25 3.33.96.1-.75.4-1.25.72-1.54-2.55-.29-5.24-1.27-5.24-5.67 0-1.25.45-2.28 1.18-3.08-.12-.29-.51-1.45.11-3.02 0 0 .96-.31 3.15 1.18a10.96 10.96 0 015.74 0c2.19-1.49 3.15-1.18 3.15-1.18.62 1.57.23 2.73.11 3.02.74.8 1.18 1.83 1.18 3.08 0 4.41-2.7 5.38-5.27 5.66.41.36.78 1.06.78 2.13v3.16c0 .31.21.67.8.56A11.5 11.5 0 0023.5 12C23.5 5.65 18.35.5 12 .5z"/></svg>
              <span>Explore all release packages, source code, and changelogs on GitHub</span>
              <span class="symbol">arrow_forward</span>
            </a>
          </div>
        </div>

        <!-- Right Side: Terminal Provider Setup Card -->
        <div class="k-terminal-card k-reveal-scale">
          <div class="k-terminal-top">
            <div class="k-window-dots">
              <span class="dot red"></span>
              <span class="dot yellow"></span>
              <span class="dot green"></span>
            </div>
            <span class="k-terminal-header-title">Provider CLI Authentication</span>
          </div>

          <div class="k-terminal-body">
            <p class="k-terminal-desc">Authenticate at least one CLI before launching Kairo:</p>
            <div class="k-cli-list">
              {
                providers.map((p) => (
                  <div
                    class="k-cli-row"
                    data-copy-cmd={p.cmd}
                    title="Click to copy command"
                  >
                    <div class="k-cli-text">
                      <span class="k-cli-prompt">$</span>
                      <code>{p.cmd}</code>
                    </div>
                    <button class="k-copy-btn" aria-label="Copy Command">
                      <span class="symbol">content_copy</span>
                    </button>
                  </div>
                ))
              }
            </div>
            <div class="k-cli-footer-note">
              <span class="symbol">check</span>
              <span>Kairo will automatically discover your active authenticated provider.</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══════════════════════════════════════════════════════════
       7. FINAL CALL TO ACTION
       ══════════════════════════════════════════════════════════ -->
  <section class="k-section k-cta-section">
    <div class="k-container">
      <div class="k-cta-box k-reveal-scale">
        <div class="k-cta-glow"></div>
        <h2>Experience a calmer, faster way to work.</h2>
        <p>Get started in seconds. No credit card required. Free and open source.</p>
        <div class="k-cta-btn-row">
          <a href="#download" class="k-btn k-btn--primary k-btn--lg">
            <span class="symbol">rocket_launch</span>
            <span>Download Kairo 2.0</span>
          </a>
          <a href="https://github.com/pingdotgg/kairo" target="_blank" class="k-btn k-btn--glass k-btn--lg">
            <span class="symbol">star</span>
            <span>Star on GitHub</span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Client-side Interactive Behaviors -->
  <script is:inline>
    // 1. Interactive App Modes Switcher
    const modeButtons = document.querySelectorAll('.k-mode-tab-btn');
    const modeViews = document.querySelectorAll('.k-mode-view');

    modeButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        const mode = btn.getAttribute('data-mode');
        modeButtons.forEach((b) => b.classList.remove('active'));
        modeViews.forEach((v) => v.classList.remove('active'));
        btn.classList.add('active');
        const activeView = document.getElementById(`view-${mode}`);
        if (activeView) activeView.classList.add('active');
      });
    });

    // 2. Interactive Prompt Simulator with Natural Typing Stream
    const simChips = document.querySelectorAll('.k-prompt-chip');
    const simInput = document.getElementById('k-sim-input');
    const simText = document.getElementById('k-sim-text');
    let typingTimer = null;

    const streamResponse = (text) => {
      if (!simText) return;
      simText.textContent = '';
      let i = 0;
      clearInterval(typingTimer);
      typingTimer = setInterval(() => {
        if (i < text.length) {
          simText.textContent += text.charAt(i);
          i++;
        } else {
          clearInterval(typingTimer);
        }
      }, 12);
    };

    const promptResponses = {
      "Summarize my unread emails and draft replies in my voice": "Found 4 actionable emails. Prepared 2 quick drafts ready for review, flagged 1 urgent contract update from finance, and cleared the remaining 5 notifications.",
      "Draft a calm vendor negotiation response using last week's contract notes": "Drafted a polite, firm renewal note highlighting our 12-month commitment and requesting the standard 15% tier discount cited in last quarter's notes.",
      "Plan my sprint schedule based on Linear tickets and upcoming calendar meetings": "Sprint mapped into 3 focused deep-work blocks on Tue/Thu morning, leaving Friday afternoon free for code review and release verification."
    };

    simChips.forEach((chip) => {
      chip.addEventListener('click', () => {
        simChips.forEach((c) => c.classList.remove('active'));
        chip.classList.add('active');
        const p = chip.getAttribute('data-prompt');
        if (p && simInput && simText) {
          simInput.value = p;
          streamResponse(promptResponses[p] || "Ready to execute with persistent context retention.");
        }
      });
    });

    // 3. One-Click Copy for CLI Terminal Commands with Global Toast
    document.querySelectorAll('.k-cli-row').forEach((row) => {
      row.addEventListener('click', () => {
        const cmd = row.getAttribute('data-copy-cmd');
        if (cmd) {
          navigator.clipboard.writeText(cmd).then(() => {
            const btn = row.querySelector('.k-copy-btn');
            if (btn) {
              btn.innerHTML = '<span class="symbol" style="color:#4ade80">check</span>';
              setTimeout(() => {
                btn.innerHTML = '<span class="symbol">content_copy</span>';
              }, 2000);
            }
            if (window.showToast) {
              window.showToast(`Copied to clipboard: ${cmd}`);
            }
          });
        }
      });
    });

    // 4. Video Autoplay, Floating Pop-out & Audio Synchronization
    const mainVideo = document.getElementById('k-main-video');
    const trailerSection = document.getElementById('trailer');
    const pipPlayer = document.getElementById('k-pip-player');
    const pipVideo = document.getElementById('k-pip-video');
    const mainSoundToggle = document.getElementById('k-main-sound-toggle');
    const mainSoundIcon = document.getElementById('k-main-sound-icon');
    const mainSoundText = document.getElementById('k-main-sound-text');
    const pipMuteBtn = document.getElementById('k-pip-mute-btn');
    const pipMuteIcon = document.getElementById('k-pip-mute-icon');
    const pipExpandBtn = document.getElementById('k-pip-expand-btn');
    const pipCloseBtn = document.getElementById('k-pip-close-btn');

    let pipDismissed = false;
    let isAudioUnmuted = false;
    let isTrailerInView = false;

    const checkTrailerVisibility = () => {
      if (!trailerSection) return false;
      const rect = trailerSection.getBoundingClientRect();
      return rect.top < window.innerHeight && rect.bottom > 0;
    };

    const triggerVideoExperience = () => {
      isTrailerInView = checkTrailerVisibility();

      if (mainVideo && mainVideo.paused) {
        mainVideo.play().catch(() => {});
      }

      if (!isTrailerInView && !pipDismissed && pipPlayer && pipVideo) {
        pipPlayer.classList.add('is-visible');
        if (mainVideo && Math.abs(pipVideo.currentTime - mainVideo.currentTime) > 1) {
          pipVideo.currentTime = mainVideo.currentTime;
        }
        if (pipVideo.paused) {
          pipVideo.play().catch(() => {});
        }
      }
    };

    ['mousemove', 'scroll', 'mouseenter', 'touchstart', 'pointerdown', 'keydown'].forEach((ev) => {
      window.addEventListener(ev, triggerVideoExperience, { passive: true });
    });
    triggerVideoExperience();

    const setAudioState = (unmuted) => {
      isAudioUnmuted = unmuted;
      if (mainVideo) mainVideo.muted = !unmuted;
      if (pipVideo) pipVideo.muted = !unmuted;

      if (mainSoundIcon && mainSoundText) {
        mainSoundIcon.textContent = unmuted ? 'volume_up' : 'volume_off';
        mainSoundText.textContent = unmuted ? 'Mute' : 'Click to unmute';
      }
      if (pipMuteIcon) {
        pipMuteIcon.textContent = unmuted ? 'volume_up' : 'volume_off';
      }
    };

    if (mainSoundToggle) {
      mainSoundToggle.addEventListener('click', () => {
        setAudioState(!isAudioUnmuted);
        if (mainVideo && mainVideo.paused) mainVideo.play().catch(() => {});
      });
    }

    if (pipMuteBtn) {
      pipMuteBtn.addEventListener('click', () => {
        setAudioState(!isAudioUnmuted);
      });
    }

    if (pipExpandBtn) {
      pipExpandBtn.addEventListener('click', () => {
        if (trailerSection) {
          trailerSection.scrollIntoView({ behavior: 'smooth' });
          if (mainVideo && pipVideo) {
            mainVideo.currentTime = pipVideo.currentTime;
            mainVideo.play().catch(() => {});
          }
        }
      });
    }

    if (pipCloseBtn) {
      pipCloseBtn.addEventListener('click', () => {
        pipDismissed = true;
        if (pipPlayer) pipPlayer.classList.remove('is-visible');
        if (pipVideo) pipVideo.pause();
      });
    }

    if (trailerSection && pipPlayer && mainVideo && pipVideo) {
      const observer = new IntersectionObserver(
        (entries) => {
          const [entry] = entries;
          isTrailerInView = entry.isIntersecting;
          if (!entry.isIntersecting && !pipDismissed) {
            pipVideo.currentTime = mainVideo.currentTime;
            pipPlayer.classList.add('is-visible');
            pipVideo.play().catch(() => {});
          } else {
            if (pipPlayer.classList.contains('is-visible')) {
              mainVideo.currentTime = pipVideo.currentTime;
              mainVideo.play().catch(() => {});
            }
            pipPlayer.classList.remove('is-visible');
          }
        },
        { threshold: 0.2 }
      );
      observer.observe(trailerSection);
    }



    // 6. Interactive Tone Presets Switcher
    const toneButtons = document.querySelectorAll('.k-tone-btn');
    const tonePreviewText = document.getElementById('k-tone-preview-text');
    const toneWordCount = document.getElementById('k-tone-word-count');
    const toneLabelEl = document.querySelector('.k-tone-tone-label');
    const copyToneBtn = document.getElementById('k-copy-tone-btn');

    const toneSamples = {
      short: {
        text: '"Thanks for the update. All approved for Thursday\'s deploy."',
        meta: '14 words · ~2s read',
        label: 'Tone: Direct & Concise'
      },
      detailed: {
        text: '"Reviewed the Q3 contract clauses and vendor metrics. Prepared a 3-point counter-proposal covering rate lock, SLA guarantees, and payment terms for review."',
        meta: '23 words · ~4s read',
        label: 'Tone: Thorough & Structured'
      },
      casual: {
        text: '"Looks awesome! Dropped a few quick notes on Slack — ship it whenever you\'re ready 🚀"',
        meta: '15 words · ~2s read',
        label: 'Tone: Friendly & Collaborative'
      },
      executive: {
        text: '"Summary: Strategic objectives on track for Q4. Resource allocation rebalanced to prioritize core reliability milestones."',
        meta: '16 words · ~3s read',
        label: 'Tone: Executive Briefing'
      }
    };

    toneButtons.forEach((btn) => {
      btn.addEventListener('click', () => {
        toneButtons.forEach((b) => b.classList.remove('active'));
        btn.classList.add('active');
        const tone = btn.getAttribute('data-tone') || 'short';
        const sample = toneSamples[tone];
        if (sample) {
          if (tonePreviewText) tonePreviewText.textContent = sample.text;
          if (toneWordCount) toneWordCount.textContent = sample.meta;
          if (toneLabelEl) toneLabelEl.textContent = sample.label;
        }
      });
    });

    if (copyToneBtn) {
      copyToneBtn.addEventListener('click', () => {
        if (tonePreviewText) {
          navigator.clipboard.writeText(tonePreviewText.textContent.replace(/^"|"$/g, '')).then(() => {
            copyToneBtn.innerHTML = '<span class="symbol" style="color:#4ade80;font-size:13px">check</span> <span>Copied</span>';
            if (window.showToast) window.showToast('Copied synthesized tone sample', 'content_copy');
            setTimeout(() => {
              copyToneBtn.innerHTML = '<span class="symbol">content_copy</span> <span>Copy</span>';
            }, 2000);
          });
        }
      });
    }

    // 7. Interactive Memory Pills Toggle
    const memPills = document.querySelectorAll('[data-mem-pill]');
    memPills.forEach((pill) => {
      pill.addEventListener('click', () => {
        pill.classList.toggle('active');
        const icon = pill.querySelector('.mem-icon');
        if (icon) {
          icon.textContent = pill.classList.contains('active') ? 'check' : 'add';
        }
      });
    });

    // 8. Card Dynamic Spotlight Glow
    const spotlightCards = document.querySelectorAll('[data-spotlight]');
    spotlightCards.forEach((card) => {
      card.addEventListener('mousemove', (e) => {
        const rect = card.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;
        card.style.setProperty('--mouse-x', `${x}px`);
        card.style.setProperty('--mouse-y', `${y}px`);
      });
    });

    // 9. Client Platform OS Detection (No recommendation tag)

    // 10. Interactive Briefing Action Check-offs
    document.querySelectorAll('.k-brief-action-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        const card = btn.closest('.k-brief-card');
        if (card) {
          card.classList.toggle('is-completed');
          if (card.classList.contains('is-completed')) {
            btn.innerHTML = '<span class="symbol" style="color:#4ade80; font-size:13px">check_circle</span> Done';
          } else {
            btn.innerHTML = 'Draft review &rarr;';
          }
        }
      });
    });

    // 11. Navbar ScrollSpy Active Link Sync
    const sections = document.querySelectorAll('section[id]');
    const navLinks = document.querySelectorAll('.ag-nav .ag-nav-btn');
    window.addEventListener('scroll', () => {
      let currentSectionId = '';
      sections.forEach((sec) => {
        const rect = sec.getBoundingClientRect();
        if (rect.top <= 140 && rect.bottom >= 140) {
          currentSectionId = sec.id;
        }
      });
      navLinks.forEach((link) => {
        const href = link.getAttribute('href');
        if (href === `#${currentSectionId}`) {
          link.classList.add('active');
        } else {
          link.classList.remove('active');
        }
      });
    }, { passive: true });

    // 12. Dynamic Real-Time Date & Greeting in Today Briefing
    const briefingDateEl = document.getElementById('k-live-briefing-date');
    if (briefingDateEl) {
      const now = new Date();
      const options = { weekday: 'long', day: 'numeric', month: 'long' };
      const formattedDate = now.toLocaleDateString('en-US', options);
      briefingDateEl.textContent = `${formattedDate} · Daily Briefing`;
    }

    // 13. Hero Simulator Engine Selector Tabs
    const enginePills = document.querySelectorAll('.k-engine-pill');
    enginePills.forEach((pill) => {
      pill.addEventListener('click', () => {
        enginePills.forEach((p) => p.classList.remove('active'));
        pill.classList.add('active');
        const engineName = pill.getAttribute('data-engine') || 'Claude Code';
        if (window.showToast) {
          window.showToast(`Switched active engine to ${engineName}`, 'tune');
        }
      });
    });

    // 14. Interactive 3D Perspective Card Tilt
    const tiltCards = document.querySelectorAll('.k-prompt-sim-card, .k-bento-card, .k-terminal-card, .k-os-card');
    tiltCards.forEach((card) => {
      card.addEventListener('mousemove', (e) => {
        const rect = card.getBoundingClientRect();
        const x = (e.clientX - rect.left) / rect.width - 0.5;
        const y = (e.clientY - rect.top) / rect.height - 0.5;
        card.style.transform = `perspective(1000px) rotateX(${y * -8}deg) rotateY(${x * 8}deg) translateY(-4px)`;
      });
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'perspective(1000px) rotateX(0deg) rotateY(0deg) translateY(0px)';
      });
    });

    // 15. Universal Scroll Reveal Engine for ALL parts of the website
    const initScrollReveal = () => {
      const revealTargets = document.querySelectorAll(
        '.k-reveal, .k-reveal-scale, .k-section-header, .k-bento-card, .k-bullet-card, .k-os-card, .k-terminal-card, .k-cta-box, .footer-col, .footer-pitch, .footer-bottom-bar'
      );

      revealTargets.forEach((el, index) => {
        const parent = el.parentElement;
        const siblings = parent ? Array.from(parent.children).filter(c => c.classList.contains('k-reveal') || c.classList.contains('k-reveal-scale') || c.classList.contains('k-bento-card') || c.classList.contains('k-bullet-card') || c.classList.contains('k-os-card')) : [];
        const siblingIndex = siblings.indexOf(el);
        const delay = siblingIndex > 0 ? `${(siblingIndex % 4) * 0.09}s` : `${(index % 3) * 0.05}s`;
        el.style.transitionDelay = delay;
      });

      const revealObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-revealed');
              entry.target.classList.add('in-view');
            }
          });
        },
        { threshold: 0.06, rootMargin: '0px 0px -40px 0px' }
      );

      revealTargets.forEach((el) => {
        revealObserver.observe(el);
        const rect = el.getBoundingClientRect();
        if (rect.top < window.innerHeight && rect.bottom > 0) {
          el.classList.add('is-revealed');
          el.classList.add('in-view');
        }
      });
    };

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initScrollReveal);
    } else {
      initScrollReveal();
    }
  </script>

  <!-- Floating Picture-in-Picture Mini Video Player -->
  <div class="k-floating-pip-player" id="k-pip-player" aria-label="Floating video player">
    <div class="k-pip-header">
      <div class="k-pip-title">
        <span class="symbol icon-purple" style="font-size:16px">play_circle</span>
        <span>Kairo in 60s</span>
      </div>
      <div class="k-pip-actions">
        <button id="k-pip-mute-btn" class="k-pip-btn" title="Toggle sound">
          <span class="symbol" id="k-pip-mute-icon">volume_off</span>
        </button>
        <button id="k-pip-expand-btn" class="k-pip-btn" title="Scroll to main video">
          <span class="symbol">fullscreen</span>
        </button>
        <button id="k-pip-close-btn" class="k-pip-btn" title="Close mini player">
          <span class="symbol">close</span>
        </button>
      </div>
    </div>
    <div class="k-pip-video-wrap">
      <video
        id="k-pip-video"
        src="/uploads/kairo-promo-v2.mp4"
        playsinline
        muted
        loop
        preload="auto"
      ></video>
    </div>
  </div>
</Layout>

<style>
  /* ══════════════════════════════════════════════════════════
     CORE DESIGN SYSTEM TOKENS & STYLES
     ══════════════════════════════════════════════════════════ */
  .k-container {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .k-section {
    padding: 100px 0;
    position: relative;
  }

  .k-section-header {
    max-width: 680px;
    margin: 0 auto 56px;
  }
  .k-text-center {
    text-align: center;
  }

  .k-section-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-family: ui-monospace, Menlo, monospace;
    font-size: 11.5px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #c084fc;
    background: rgba(192, 132, 252, 0.1);
    border: 1px solid rgba(192, 132, 252, 0.25);
    padding: 5px 14px;
    border-radius: 20px;
    margin-bottom: 16px;
  }

  .k-section-heading {
    font-size: clamp(30px, 4.5vw, 46px);
    font-weight: 800;
    line-height: 1.15;
    letter-spacing: -0.03em;
    color: #ffffff;
    margin: 0 0 14px;
  }

  .k-section-lead {
    font-size: 17px;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    margin: 0;
  }

  /* ══════════════════════════════════════════════════════════
     1. HERO STAGE
     ══════════════════════════════════════════════════════════ */
  .k-hero-stage {
    position: relative;
    min-height: 700px;
    display: flex;
    align-items: center;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding: 100px 0 80px;
    overflow: hidden;
  }

  .k-hero-container {
    position: relative;
    z-index: 2;
    width: 100%;
  }

  .k-hero-layout-grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 44px;
    align-items: center;
  }

  .k-hero-left {
    display: flex;
    flex-direction: column;
    gap: 22px;
    color: #ffffff;
  }

  .k-hero-right {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .k-hero-trust-row {
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    margin-top: 4px;
  }

  .k-trust-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12.5px;
    color: rgba(255, 255, 255, 0.7);
  }

  .k-live-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(49, 134, 255, 0.35);
    padding: 6px 14px;
    border-radius: 20px;
    align-self: flex-start;
  }

  .k-pulse-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #00B95C;
    box-shadow: 0 0 10px #00B95C;
    animation: kPulse 2s infinite;
  }
  @keyframes kPulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.85); }
  }

  .k-badge-text {
    font-family: ui-monospace, Menlo, monospace;
    font-size: 11px;
    letter-spacing: 0.14em;
    color: rgba(147, 197, 253, 0.95);
    font-weight: 600;
  }

  .k-hero-title {
    font-size: clamp(40px, 6vw, 62px);
    font-weight: 850;
    line-height: 1.08;
    letter-spacing: -0.03em;
    margin: 0;
  }

  .k-title-gradient {
    background: linear-gradient(90deg, #ffffff 0%, #93c5fd 25%, #60a5fa 50%, #93c5fd 75%, #ffffff 100%);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: kTextShimmer 9s linear infinite;
  }
  @keyframes kTextShimmer {
    to { background-position: 200% center; }
  }

  .k-hero-subtitle {
    font-size: 18px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.82);
    margin: 0;
  }

  .k-hero-actions {
    display: flex;
    gap: 14px;
    align-items: center;
    flex-wrap: wrap;
    margin-top: 4px;
  }

  .k-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    border-radius: 12px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
  }

  .k-btn--lg {
    padding: 13px 26px;
    font-size: 15px;
  }

  .k-btn--sm {
    padding: 6px 14px;
    font-size: 12.5px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
    color: #fff;
  }
  .k-btn--sm:hover {
    background: rgba(255, 255, 255, 0.16);
  }

  .k-btn--primary {
    background: #ffffff;
    color: #08090e;
    border: 1px solid rgba(255, 255, 255, 0.95);
    box-shadow: 0 4px 18px rgba(0, 0, 0, 0.45);
    font-weight: 700;
  }
  .k-btn--primary:hover {
    background: #f1f5f9;
    color: #000000;
    transform: translateY(-2px);
    box-shadow: 0 8px 26px rgba(0, 0, 0, 0.6);
  }

  /* Universal Scroll Reveal Animation Styles */
  .k-reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
    will-change: opacity, transform;
  }
  .k-reveal.is-revealed,
  .k-reveal.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  .k-reveal-scale {
    opacity: 0;
    transform: translateY(24px) scale(0.97);
    transition: opacity 0.75s cubic-bezier(0.16, 1, 0.3, 1), transform 0.75s cubic-bezier(0.16, 1, 0.3, 1);
    will-change: opacity, transform;
  }
  .k-reveal-scale.is-revealed,
  .k-reveal-scale.in-view {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  .k-reveal-on-scroll {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
    will-change: opacity, transform;
  }
  .k-reveal-on-scroll.is-revealed,
  .k-reveal-on-scroll.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  .k-btn--glass {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(10px);
    border: 1.5px solid rgba(255, 255, 255, 0.35);
    color: #ffffff;
  }
  .k-btn--glass:hover {
    background: rgba(255, 255, 255, 0.16);
    border-color: rgba(255, 255, 255, 0.7);
    transform: translateY(-2px);
  }

  .k-play-badge {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 11px;
    padding-left: 2px;
  }

  .k-btn-text-group {
    display: flex;
    flex-direction: column;
    text-align: left;
  }
  .k-btn-main { font-size: 14px; line-height: 1.2; }
  .k-btn-sub { font-size: 11px; color: rgba(255, 255, 255, 0.6); }

  .k-handwritten-note {
    font-family: 'Caveat', cursive;
    font-size: 19px;
    color: #d8b4fe;
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 2px;
  }

  /* Interactive Prompt Simulator Card */
  .k-prompt-sim-card {
    position: relative;
    margin-top: 14px;
    background: rgba(18, 8, 36, 0.9);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(214, 186, 255, 0.3);
    border-radius: 18px;
    padding: 18px;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6), 0 0 30px rgba(168, 85, 247, 0.25);
    overflow: hidden;
    z-index: 1;
  }
  .k-prompt-sim-card::before,
  .k-bento-card.featured::before {
    content: '';
    position: absolute;
    inset: -150%;
    background: conic-gradient(
      from 0deg,
      transparent 0deg 300deg,
      rgba(125, 211, 252, 0.7) 330deg,
      rgba(192, 132, 252, 0.9) 360deg
    );
    animation: kConicSpin 6s linear infinite;
    pointer-events: none;
    z-index: -1;
  }
  .k-prompt-sim-card::after,
  .k-bento-card.featured::after {
    content: '';
    position: absolute;
    inset: 1px;
    background: #0d0818;
    border-radius: inherit;
    z-index: -1;
  }
  @keyframes kConicSpin {
    100% { transform: rotate(360deg); }
  }

  .k-float-levitate {
    animation: kFloatSlow 5s ease-in-out infinite alternate;
  }
  @keyframes kFloatSlow {
    0% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-7px) rotate(1deg); }
    100% { transform: translateY(0px) rotate(0deg); }
  }

  .k-prompt-sim-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 12px;
  }

  .k-sim-engine-pills {
    display: flex;
    gap: 6px;
  }
  .k-engine-pill {
    font-size: 11px;
    padding: 3px 10px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.6);
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .k-engine-pill:hover {
    color: #ffffff;
    border-color: rgba(168, 85, 247, 0.4);
  }
  .k-engine-pill.active {
    background: rgba(168, 85, 247, 0.2);
    border-color: rgba(168, 85, 247, 0.6);
    color: #c084fc;
    font-weight: 600;
  }

  .k-water-ripple-ring {
    position: absolute;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    border: 1.5px solid rgba(125, 211, 252, 0.75);
    box-shadow: 0 0 24px rgba(125, 211, 252, 0.45);
    transform: translate(-50%, -50%) scale(0);
    pointer-events: none;
    animation: kRippleExpand 1.4s cubic-bezier(0.1, 0.8, 0.3, 1) forwards;
    z-index: 10;
  }
  @keyframes kRippleExpand {
    0% {
      transform: translate(-50%, -50%) scale(0);
      opacity: 0.95;
    }
    100% {
      transform: translate(-50%, -50%) scale(38);
      opacity: 0;
    }
  }

  .k-window-dots {
    display: flex;
    gap: 5px;
  }
  .dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }
  .dot.red { background: #ef4444; }
  .dot.yellow { background: #f59e0b; }
  .dot.green { background: #10b981; }

  .k-prompt-sim-title {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 500;
  }

  .k-prompt-input-row {
    display: flex;
    align-items: center;
    gap: 10px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 12px;
    padding: 6px 10px 6px 12px;
  }

  .k-prompt-icon-bubble {
    color: #c084fc;
    display: flex;
    align-items: center;
  }

  .k-sim-text-input {
    flex: 1;
    background: transparent;
    border: none;
    color: #ffffff;
    font-size: 13.5px;
    outline: none;
    font-family: inherit;
  }

  .k-sim-send-btn {
    background: #a855f7;
    border: none;
    border-radius: 8px;
    width: 32px;
    height: 32px;
    color: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: background 0.2s;
  }
  .k-sim-send-btn:hover {
    background: #9333ea;
  }

  .k-prompt-chips {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 10px;
    flex-wrap: wrap;
  }

  .k-chip-label {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.5);
  }

  .k-prompt-chip {
    background: rgba(255, 255, 255, 0.05);
    border: 1px dashed rgba(255, 255, 255, 0.2);
    color: rgba(255, 255, 255, 0.8);
    font-size: 11.5px;
    padding: 3px 10px;
    border-radius: 14px;
    cursor: pointer;
    transition: all 0.15s;
  }
  .k-prompt-chip:hover, .k-prompt-chip.active {
    background: rgba(168, 85, 247, 0.25);
    border-color: #c084fc;
    color: #ffffff;
  }

  .k-prompt-sim-result {
    margin-top: 12px;
    padding-top: 12px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .k-sim-res-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
  }

  .k-memory-tag {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    color: #4ade80;
    background: rgba(74, 222, 128, 0.1);
    padding: 2px 8px;
    border-radius: 6px;
  }

  .k-sim-time {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.4);
  }

  .k-sim-res-text {
    font-size: 13px;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.85);
    margin: 0;
  }

  /* ══════════════════════════════════════════════════════════
     2. VIDEO SHOWCASE SECTION
     ══════════════════════════════════════════════════════════ */
  .k-video-section {
    background: #090a0f;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .k-video-player-wrapper {
    position: relative;
    max-width: 980px;
    margin: 0 auto 48px;
  }

  .k-video-glow {
    position: absolute;
    inset: -10px;
    background: radial-gradient(circle, rgba(168, 85, 247, 0.25) 0%, transparent 70%);
    filter: blur(30px);
    z-index: 0;
  }

  .k-video-frame {
    position: relative;
    z-index: 1;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    background: #000000;
    overflow: hidden;
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.8);
  }

  .k-hero-video {
    width: 100%;
    height: 520px;
    object-fit: cover;
    display: block;
    pointer-events: none;
    user-select: none;
  }

  /* ══════════════════════════════════════════════════════════
     FLOATING PICTURE-IN-PICTURE (PIP) MINI PLAYER
     ══════════════════════════════════════════════════════════ */
  .k-floating-pip-player {
    position: fixed;
    bottom: 28px;
    right: 28px;
    width: 320px;
    background: #14131d;
    border: 1px solid rgba(168, 85, 247, 0.35);
    border-radius: 16px;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.8), 0 0 30px rgba(168, 85, 247, 0.2);
    overflow: hidden;
    z-index: 9999;
    opacity: 0;
    pointer-events: none;
    transform: translateY(30px) scale(0.95);
    transition: opacity 0.35s cubic-bezier(0.16, 1, 0.3, 1),
                transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .k-floating-pip-player.is-visible {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0) scale(1);
  }

  .k-floating-pip-player:hover {
    border-color: rgba(168, 85, 247, 0.6);
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.9), 0 0 40px rgba(168, 85, 247, 0.3);
  }

  .k-pip-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 12px;
    background: rgba(255, 255, 255, 0.03);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .k-pip-title {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
  }

  .k-pip-actions {
    display: flex;
    gap: 4px;
    align-items: center;
  }

  .k-pip-btn {
    width: 26px;
    height: 26px;
    border-radius: 6px;
    background: transparent;
    border: none;
    color: rgba(255, 255, 255, 0.7);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.15s ease;
  }
  .k-pip-btn .symbol {
    font-size: 16px;
  }
  .k-pip-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    color: #fff;
  }

  .k-pip-video-wrap {
    width: 100%;
    height: 180px;
    background: #000;
    position: relative;
  }

  .k-pip-video-wrap video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .k-video-bullets-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    max-width: 980px;
    margin: 0 auto;
  }

  .k-bullet-card {
    background: rgba(255, 255, 255, 0.025);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    padding: 22px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .k-bullet-icon {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: rgba(168, 85, 247, 0.12);
    border: 1px solid rgba(168, 85, 247, 0.25);
    color: #c084fc;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .k-bullet-card h4 {
    font-size: 16px;
    font-weight: 700;
    color: #ffffff;
    margin: 0;
  }

  .k-bullet-card p {
    font-size: 13.5px;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.65);
    margin: 0;
  }

  /* ══════════════════════════════════════════════════════════
     3. BENTO GRID FEATURES
     ══════════════════════════════════════════════════════════ */
  .k-features-section {
    background: #0d0e14;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .k-features-top-bar {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 40px;
    margin-bottom: 40px;
    flex-wrap: wrap;
  }

  .k-features-subhead {
    font-size: 16px;
    color: rgba(255, 255, 255, 0.65);
    max-width: 440px;
    line-height: 1.5;
    margin: 0;
  }

  .k-bento-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
  }

  .k-bento-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 18px;
    padding: 22px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  [data-spotlight] {
    position: relative;
    overflow: hidden;
  }
  [data-spotlight]::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: radial-gradient(
      380px circle at var(--mouse-x, -999px) var(--mouse-y, -999px),
      rgba(168, 85, 247, 0.18),
      transparent 60%
    );
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
    z-index: 0;
  }
  [data-spotlight]:hover::before {
    opacity: 1;
  }
  [data-spotlight] > * {
    position: relative;
    z-index: 1;
  }

  .k-bento-card:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(168, 85, 247, 0.35);
    transform: translateY(-3px);
    box-shadow: 0 16px 36px rgba(0, 0, 0, 0.4);
  }

  .k-bento-wide {
    grid-column: span 3;
    background: linear-gradient(135deg, rgba(168, 85, 247, 0.06) 0%, rgba(255, 255, 255, 0.02) 100%);
  }

  .k-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .k-icon-box {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .k-icon-purple { background: rgba(168, 85, 247, 0.15); color: #c084fc; border: 1px solid rgba(168, 85, 247, 0.3); }
  .k-icon-blue { background: rgba(59, 130, 246, 0.15); color: #60a5fa; border: 1px solid rgba(59, 130, 246, 0.3); }
  .k-icon-green { background: rgba(16, 185, 129, 0.15); color: #34d399; border: 1px solid rgba(16, 185, 129, 0.3); }
  .k-icon-yellow { background: rgba(245, 158, 11, 0.15); color: #fbbf24; border: 1px solid rgba(245, 158, 11, 0.3); }

  .k-card-tag {
    font-size: 11px;
    font-family: ui-monospace, Menlo, monospace;
    color: rgba(255, 255, 255, 0.5);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .k-card-title {
    font-size: 18px;
    font-weight: 700;
    color: #ffffff;
    margin: 0;
  }

  .k-card-desc {
    font-size: 13.5px;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.65);
    margin: 0;
  }

  /* Document Intelligence Preview Widget */
  .k-doc-preview-widget {
    margin-top: auto;
    background: rgba(0, 0, 0, 0.35);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    padding: 12px 14px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .k-doc-header-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 8px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }
  .k-doc-file-info {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .k-pdf-icon {
    color: #f87171;
    font-size: 20px;
  }
  .k-doc-filename {
    display: block;
    font-size: 12.5px;
    color: #ffffff;
  }
  .k-doc-meta {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.45);
  }
  .k-status-badge-green {
    font-size: 10.5px;
    font-weight: 600;
    color: #4ade80;
    background: rgba(74, 222, 128, 0.12);
    border: 1px solid rgba(74, 222, 128, 0.3);
    padding: 2px 7px;
    border-radius: 4px;
  }
  .k-doc-insights-list {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .k-insight-item {
    display: flex;
    align-items: flex-start;
    gap: 6px;
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.4;
  }
  .k-check-icon {
    font-size: 14px;
    color: #4ade80;
    margin-top: 1px;
    flex-shrink: 0;
  }
  .k-doc-footer-meta {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    color: #c084fc;
    background: rgba(168, 85, 247, 0.08);
    padding: 4px 8px;
    border-radius: 6px;
  }

  /* Tone & Voice Widget */
  .k-tone-widget {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .k-tone-switcher {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }
  .k-tone-btn {
    font-size: 11.5px;
    padding: 4px 10px;
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: rgba(255, 255, 255, 0.7);
    cursor: pointer;
    transition: all 0.15s;
  }
  .k-tone-btn:hover {
    background: rgba(255, 255, 255, 0.12);
    color: #ffffff;
  }
  .k-tone-btn.active {
    background: #ffffff;
    color: #000000;
    font-weight: 600;
  }
  .k-tone-preview-box {
    background: rgba(0, 0, 0, 0.35);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    padding: 12px 14px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .k-tone-quote-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .k-tone-meta-pill {
    font-size: 10.5px;
    color: rgba(255, 255, 255, 0.45);
    font-family: ui-monospace, Menlo, monospace;
  }
  #k-tone-preview-text {
    font-size: 12.5px;
    line-height: 1.45;
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
    font-style: italic;
  }
  .k-tone-preview-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 6px;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }
  .k-tone-tone-label {
    font-size: 11px;
    color: #60a5fa;
  }
  .k-copy-snippet-btn {
    display: flex;
    align-items: center;
    gap: 4px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    color: rgba(255, 255, 255, 0.8);
    padding: 2px 8px;
    border-radius: 6px;
    font-size: 11px;
    cursor: pointer;
    transition: all 0.15s;
  }
  .k-copy-snippet-btn:hover {
    background: rgba(255, 255, 255, 0.15);
    color: #ffffff;
  }
  .k-copy-snippet-btn .symbol { font-size: 13px; }

  /* Supermemory Widget */
  .k-memory-widget {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .k-memory-chips-demo {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .k-mem-pill {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(0, 0, 0, 0.35);
    border: 1px solid rgba(255, 255, 255, 0.08);
    padding: 8px 10px;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.15s;
  }
  .k-mem-pill:hover {
    border-color: rgba(96, 165, 250, 0.4);
    background: rgba(96, 165, 250, 0.06);
  }
  .k-mem-pill.active {
    border-color: rgba(96, 165, 250, 0.35);
    background: rgba(96, 165, 250, 0.08);
  }
  .k-mem-pill-left {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
  }
  .k-mem-pill-left .mem-icon {
    font-size: 14px;
    color: #60a5fa;
  }
  .k-mem-key {
    color: #ffffff;
    margin-right: 4px;
  }
  .k-mem-val {
    color: rgba(255, 255, 255, 0.7);
  }
  .k-mem-category-tag {
    font-size: 10px;
    font-family: ui-monospace, Menlo, monospace;
    background: rgba(255, 255, 255, 0.08);
    color: rgba(255, 255, 255, 0.6);
    padding: 1px 6px;
    border-radius: 4px;
  }
  .k-memory-footer-note {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    color: rgba(255, 255, 255, 0.45);
    margin-top: 4px;
  }

  /* Connected Tools Widget */
  .k-tools-grid-preview {
    margin-top: auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
  }
  .k-tool-chip {
    background: rgba(0, 0, 0, 0.35);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
    padding: 8px 10px;
    display: flex;
    align-items: center;
    gap: 8px;
    position: relative;
  }
  .k-tool-chip .symbol {
    font-size: 18px;
  }
  .k-tool-info {
    display: flex;
    flex-direction: column;
    font-size: 11.5px;
  }
  .k-tool-info strong {
    color: #ffffff;
  }
  .k-tool-info span {
    font-size: 10.5px;
    color: rgba(255, 255, 255, 0.5);
  }
  .k-tool-status-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #4ade80;
    margin-left: auto;
    box-shadow: 0 0 8px #4ade80;
  }
  .k-tools-footer-badge {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    color: #fbbf24;
    background: rgba(245, 158, 11, 0.08);
    border: 1px solid rgba(245, 158, 11, 0.2);
    padding: 5px 10px;
    border-radius: 6px;
    margin-top: 4px;
  }

  /* Wide Multi-Provider Card */
  .k-wide-layout {
    display: grid;
    grid-template-columns: 1fr 1.3fr;
    gap: 32px;
    align-items: center;
  }
  .k-wide-metrics-row {
    display: flex;
    gap: 12px;
    margin-top: 14px;
  }
  .k-metric-pill {
    background: rgba(0, 0, 0, 0.4);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    padding: 6px 12px;
    display: flex;
    flex-direction: column;
  }
  .k-metric-pill strong {
    font-size: 15px;
    color: #c084fc;
  }
  .k-metric-pill span {
    font-size: 10.5px;
    color: rgba(255, 255, 255, 0.5);
  }
  .k-provider-cards-col {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .k-provider-mini-card {
    background: rgba(0, 0, 0, 0.4);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
    padding: 10px 14px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
  }
  .k-pm-left {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .k-pm-title-row {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .k-pm-name {
    font-size: 13.5px;
    color: #ffffff;
  }
  .k-pm-desc {
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.55);
  }
  .k-cli-shortcut-tag {
    font-size: 11px;
    font-family: ui-monospace, Menlo, monospace;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.7);
    padding: 3px 8px;
    border-radius: 6px;
    white-space: nowrap;
  }

  .k-badge {
    font-size: 10.5px;
    padding: 2px 7px;
    border-radius: 5px;
    font-weight: 600;
    flex-shrink: 0;
  }
  .badge-purple { background: rgba(168, 85, 247, 0.2); color: #c084fc; border: 1px solid rgba(168, 85, 247, 0.4); }
  .badge-blue { background: rgba(59, 130, 246, 0.2); color: #60a5fa; border: 1px solid rgba(59, 130, 246, 0.4); }
  .badge-green { background: rgba(16, 185, 129, 0.2); color: #34d399; border: 1px solid rgba(16, 185, 129, 0.4); }
  .badge-yellow { background: rgba(245, 158, 11, 0.2); color: #fbbf24; border: 1px solid rgba(245, 158, 11, 0.4); }

  .k-info-banner {
    margin-top: 24px;
    border: 1.5px dashed rgba(168, 85, 247, 0.35);
    background: rgba(168, 85, 247, 0.05);
    border-radius: 14px;
    padding: 14px 20px;
    display: flex;
    align-items: center;
    gap: 14px;
    font-size: 13.5px;
    color: rgba(255, 255, 255, 0.8);
  }

  /* ══════════════════════════════════════════════════════════
     4. INTERFACE MODES EXPLORER
     ══════════════════════════════════════════════════════════ */
  .k-modes-section {
    background: #090a0f;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .k-mode-tabs-row {
    display: flex;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
    margin-bottom: 28px;
  }

  .k-mode-tab-btn {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 8px 16px;
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
    color: rgba(255, 255, 255, 0.7);
  }
  .k-mode-tab-btn:hover {
    background: rgba(255, 255, 255, 0.08);
    color: #fff;
  }
  .k-mode-tab-btn.active {
    background: rgba(168, 85, 247, 0.2);
    border-color: #c084fc;
    color: #ffffff;
    box-shadow: 0 4px 18px rgba(168, 85, 247, 0.25);
  }

  .k-tab-pill {
    font-size: 10px;
    font-family: ui-monospace, Menlo, monospace;
    background: rgba(255, 255, 255, 0.1);
    padding: 2px 6px;
    border-radius: 4px;
  }
  .k-tab-name { font-size: 13.5px; font-weight: 600; }

  .k-mode-stage-wrapper {
    max-width: 960px;
    margin: 0 auto;
    background: #13141c;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.6);
  }

  .k-mode-view {
    display: none;
    animation: kFadeIn 0.3s ease;
  }
  .k-mode-view.active {
    display: block;
  }
  @keyframes kFadeIn {
    from { opacity: 0; transform: scale(0.99); }
    to { opacity: 1; transform: scale(1); }
  }

  /* Calm Canvas View */
  .k-canvas-inner {
    padding: 20px 24px;
    min-height: 400px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }
  .k-canvas-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding-bottom: 12px;
  }
  .k-app-brand {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    font-size: 15px;
  }
  .k-canvas-mode-tag {
    font-size: 11px;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.5);
    background: rgba(255, 255, 255, 0.06);
    padding: 2px 7px;
    border-radius: 4px;
  }
  .k-canvas-actions {
    display: flex;
    align-items: center;
    gap: 12px;
    color: rgba(255, 255, 255, 0.6);
  }
  .k-status-indicator {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11.5px;
    color: #4ade80;
  }
  .k-pulse-dot-sm {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #4ade80;
    box-shadow: 0 0 8px #4ade80;
  }

  .k-canvas-body {
    max-width: 640px;
    margin: 24px auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }
  .k-canvas-intro {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .k-canvas-eyebrow {
    font-size: 13px;
    color: #c084fc;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-family: ui-monospace, Menlo, monospace;
  }
  .k-canvas-greeting {
    font-size: 22px;
    font-weight: 700;
    color: #ffffff;
    margin: 0;
  }
  .k-canvas-input-box {
    width: 100%;
    background: rgba(0, 0, 0, 0.4);
    border: 1.5px solid rgba(168, 85, 247, 0.3);
    border-radius: 14px;
    padding: 14px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
  }
  .k-canvas-input-top {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .k-canvas-input-box input {
    flex: 1;
    background: transparent;
    border: none;
    color: #fff;
    font-size: 14px;
    outline: none;
  }
  .k-canvas-bottom-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .k-canvas-tools {
    display: flex;
    gap: 8px;
  }
  .k-tool-btn {
    display: flex;
    align-items: center;
    gap: 4px;
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.7);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 3px 8px;
    border-radius: 6px;
  }
  .k-tool-btn .symbol { font-size: 14px; }
  .k-canvas-send-btn {
    width: 30px;
    height: 30px;
    border-radius: 8px;
    background: #ffffff;
    color: #000000;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
  }
  .k-canvas-chips-row {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
    justify-content: center;
  }
  .k-canvas-result-card {
    width: 100%;
    background: rgba(255, 255, 255, 0.025);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 10px;
    padding: 10px 14px;
    text-align: left;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .k-canvas-result-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .k-canvas-result-desc {
    font-size: 12.5px;
    line-height: 1.45;
    color: rgba(255, 255, 255, 0.85);
    margin: 0;
  }

  /* Workspace Rails View */
  .k-workspace-inner {
    display: grid;
    grid-template-columns: 230px 1fr;
    min-height: 420px;
  }
  .k-ws-sidebar {
    background: rgba(0, 0, 0, 0.35);
    border-right: 1px solid rgba(255, 255, 255, 0.08);
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .k-new-btn {
    background: rgba(168, 85, 247, 0.15);
    border: 1px solid rgba(168, 85, 247, 0.3);
    border-radius: 8px;
    padding: 8px 12px;
    color: #fff;
    font-size: 12.5px;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 6px;
    cursor: pointer;
  }
  .k-new-btn .symbol { font-size: 16px; color: #c084fc; }
  .k-thread-list {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .k-thread-item {
    font-size: 12px;
    padding: 8px 10px;
    border-radius: 8px;
    color: rgba(255, 255, 255, 0.7);
    display: flex;
    flex-direction: column;
    gap: 2px;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid transparent;
    cursor: pointer;
  }
  .k-thread-item.active {
    background: rgba(168, 85, 247, 0.15);
    border-color: rgba(168, 85, 247, 0.35);
    color: #ffffff;
  }
  .k-thread-title-row {
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .k-thread-status-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
  }
  .k-thread-status-dot.green { background: #4ade80; }
  .k-thread-status-dot.purple { background: #c084fc; }
  .k-thread-status-dot.blue { background: #60a5fa; }
  .k-thread-name {
    font-weight: 600;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .k-thread-time {
    font-size: 10.5px;
    color: rgba(255, 255, 255, 0.4);
    padding-left: 12px;
  }
  .k-ws-bottom-connected {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding-top: 10px;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }
  .k-conn-chips-list {
    display: flex;
    gap: 6px;
  }
  .k-conn-badge {
    display: flex;
    align-items: center;
    gap: 4px;
    font-size: 10.5px;
    background: rgba(255, 255, 255, 0.06);
    padding: 2px 6px;
    border-radius: 4px;
    color: rgba(255, 255, 255, 0.7);
  }
  .k-conn-badge .symbol { font-size: 12px; }

  .k-ws-main {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 18px;
    background: rgba(0, 0, 0, 0.2);
  }
  .k-ws-chat-area {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .k-chat-bubble {
    max-width: 85%;
    padding: 12px 16px;
    border-radius: 12px;
    font-size: 13.5px;
    line-height: 1.5;
  }
  .k-bubble-author {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 4px;
  }
  .k-bubble-author-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
  }
  .k-chat-bubble.user {
    align-self: flex-end;
    background: rgba(168, 85, 247, 0.18);
    border: 1px solid rgba(168, 85, 247, 0.35);
    color: #ffffff;
  }
  .k-chat-bubble.assistant {
    align-self: flex-start;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #ffffff;
  }
  .k-ws-pricing-table {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin: 8px 0;
  }
  .k-pricing-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(0, 0, 0, 0.3);
    padding: 6px 10px;
    border-radius: 6px;
    font-size: 12px;
  }
  .k-pricing-row.highlight {
    border: 1px solid rgba(74, 222, 128, 0.3);
    background: rgba(74, 222, 128, 0.08);
  }
  .k-provider-cost {
    font-weight: 700;
    color: #4ade80;
  }
  .k-provider-note {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.5);
  }
  .k-ws-saved-memory-alert {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11.5px;
    color: #60a5fa;
    background: rgba(96, 165, 250, 0.1);
    border: 1px solid rgba(96, 165, 250, 0.25);
    padding: 5px 8px;
    border-radius: 6px;
    margin-top: 6px;
  }

  /* Today Briefing View */
  .k-briefing-inner {
    padding: 24px;
  }
  .k-briefing-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding-bottom: 14px;
    margin-bottom: 20px;
  }
  .k-briefing-subtitle {
    font-size: 12.5px;
    color: rgba(255, 255, 255, 0.5);
  }
  .k-briefing-cards-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
  }
  .k-brief-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    transition: all 0.25s ease;
  }
  .k-brief-card.wide { grid-column: span 2; }
  .k-brief-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: auto;
    padding-top: 8px;
  }
  .k-brief-time {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.45);
  }

  /* Split View */
  .k-split-inner {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    min-height: 380px;
  }
  .k-split-left {
    padding: 20px;
    border-right: 1px solid rgba(255, 255, 255, 0.08);
  }
  .k-split-pane-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
  }
  .k-split-pane-header h4 {
    font-size: 14.5px;
    font-weight: 700;
    color: #ffffff;
    margin: 0;
  }
  .k-split-lead {
    font-size: 12.5px;
    color: rgba(255, 255, 255, 0.65);
    margin: 0 0 10px;
  }
  .k-code-snippet-box {
    background: #090a0f;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 6px;
    padding: 8px 10px;
    font-family: ui-monospace, Menlo, monospace;
    font-size: 11px;
    color: #c084fc;
    margin: 8px 0;
    overflow-x: auto;
  }
  .k-split-right {
    padding: 20px;
    background: rgba(0, 0, 0, 0.2);
  }
  .k-source-item strong {
    color: #ffffff;
    display: block;
  }
  .k-source-item span {
    font-size: 11px;
    color: rgba(255, 255, 255, 0.45);
  }

  /* ══════════════════════════════════════════════════════════
     5. DOWNLOAD & TERMINAL SECTION
     ══════════════════════════════════════════════════════════ */
  .k-download-section {
    background: #0d0e14;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .k-download-layout {
    display: grid;
    grid-template-columns: 1.2fr 0.9fr;
    gap: 48px;
    align-items: center;
  }

  .k-download-lead {
    font-size: 16.5px;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    margin: 0 0 28px;
  }

  .k-os-cards-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-bottom: 24px;
  }

  .k-os-card {
    position: relative;
    background: #11131b;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 18px 20px;
    display: flex;
    align-items: center;
    gap: 14px;
    text-decoration: none;
    color: #ffffff;
    transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .k-os-card:hover {
    background: #191d2e;
    border-color: rgba(49, 134, 255, 0.7);
    transform: translateY(-2px);
  }

  .k-os-icon {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    background: rgba(49, 134, 255, 0.12);
    border: 1px solid rgba(49, 134, 255, 0.25);
    color: #3186FF;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .k-os-details {
    display: flex;
    flex-direction: column;
    flex: 1;
  }
  .k-os-title { font-size: 14.5px; font-weight: 700; }
  .k-os-meta { font-size: 11.5px; color: rgba(255, 255, 255, 0.5); }
  .k-download-arrow { color: rgba(255, 255, 255, 0.4); font-size: 18px; }

  .k-gh-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 13.5px;
    color: #c084fc;
    text-decoration: none;
  }
  .k-gh-link:hover { text-decoration: underline; }

  /* Terminal Card */
  .k-terminal-card {
    background: #111218;
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
  }

  .k-terminal-top {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 18px;
    background: rgba(255, 255, 255, 0.04);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }
  .k-terminal-header-title {
    font-size: 12.5px;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.7);
  }

  .k-terminal-body {
    padding: 20px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .k-terminal-desc {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
  }

  .k-cli-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .k-cli-row {
    background: rgba(0, 0, 0, 0.4);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    padding: 10px 14px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    cursor: pointer;
    transition: all 0.15s ease;
  }
  .k-cli-row:hover {
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(168, 85, 247, 0.4);
  }

  .k-cli-text {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .k-cli-prompt { color: #a855f7; font-family: 'JetBrains Mono', monospace; font-weight: 700; }
  .k-cli-row code {
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    color: #e2e8f0;
  }

  .k-copy-btn {
    background: none;
    border: none;
    color: rgba(255, 255, 255, 0.4);
    cursor: pointer;
    display: flex;
    align-items: center;
  }
  .k-cli-row:hover .k-copy-btn { color: #ffffff; }

  .k-cli-footer-note {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11.5px;
    color: #4ade80;
  }



  /* ══════════════════════════════════════════════════════════
     7. FINAL CTA
     ══════════════════════════════════════════════════════════ */
  .k-cta-section {
    background: #0d0e14;
    padding: 120px 0;
  }

  .k-cta-box {
    position: relative;
    background: radial-gradient(100% 100% at 50% 0%, #431f7d 0%, #170b2e 100%);
    border: 1px solid rgba(168, 85, 247, 0.35);
    border-radius: 28px;
    padding: 64px 32px;
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 18px;
    overflow: hidden;
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.6);
  }

  .k-cta-glow {
    position: absolute;
    top: -50%;
    left: 50%;
    transform: translateX(-50%);
    width: 400px;
    height: 250px;
    background: rgba(192, 132, 252, 0.35);
    filter: blur(80px);
    pointer-events: none;
  }

  .k-cta-box h2 {
    font-size: clamp(32px, 5vw, 44px);
    font-weight: 800;
    color: #ffffff;
    margin: 0;
  }

  .k-cta-box p {
    font-size: 17px;
    color: rgba(255, 255, 255, 0.8);
    max-width: 520px;
    margin: 0;
  }

  .k-cta-btn-row {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 12px;
  }

  /* ══════════════════════════════════════════════════════════
     RESPONSIVE DESIGN BREAKPOINTS
     ══════════════════════════════════════════════════════════ */
  @media (max-width: 992px) {
    .k-hero-layout-grid { grid-template-columns: 1fr; gap: 36px; }
    .k-bento-grid { grid-template-columns: 1fr 1fr; }
    .k-bento-wide { grid-column: span 2; }
    .k-wide-layout { grid-template-columns: 1fr; }
    .k-download-layout { grid-template-columns: 1fr; }
    .k-video-bullets-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 768px) {
    .k-hero-stage { padding: 60px 0; }
    .k-hero-title { font-size: 36px; }
    .k-bento-grid { grid-template-columns: 1fr; }
    .k-bento-wide { grid-column: span 1; }
    .k-os-cards-grid { grid-template-columns: 1fr; }
    .k-workspace-inner { grid-template-columns: 1fr; }
    .k-briefing-cards-grid { grid-template-columns: 1fr; }
    .k-brief-card.wide { grid-column: span 1; }
    .k-split-inner { grid-template-columns: 1fr; }
    .k-hero-video { height: 260px; }
    .k-floating-pip-player { width: 230px; bottom: 16px; right: 16px; }
    .k-pip-video-wrap { height: 130px; }
  }
</style>
